
# HealthCore — Bitácora de Desarrollo

Documento vivo que registra el avance real del proyecto (qué se hizo, en qué orden, y por qué). Se actualiza a medida que se implementa. Para el plan original y las decisiones de arquitectura, ver el DRF (`DRF_Plataforma_IPS_v1_0_APA.pdf`) y el resumen de decisiones más abajo.

## Stack y decisiones fijadas

- Backend: Node.js + Express 5, PostgreSQL, **Prisma ORM** (migraciones versionadas).
- Frontend: React 19 + Vite, **CSS plano / CSS Modules** (sin Tailwind ni librería de componentes).
- Alcance de esta iteración: **CRUD de usuarios** (RF-ADM-01/02/03), sin login/JWT todavía. Contraseñas hasheadas con `bcryptjs`.
- Postgres corre en **Docker** (`docker-compose.yml` en la raíz).
- Diseño fuente de verdad: pantalla "Crear / Editar Usuario" importada desde Claude Design (proyecto `5e1ee8b8-0b03-4662-aad7-0759527a4c95`, archivo `Gestion de Usuarios.dc.html`).
- Plan detallado completo: `C:\Users\USUARIO\.claude\plans\use-the-claude-design-mcp-cozy-parrot.md`.

## Avance

### 2026-07-14

- [x] Explorado el repo: `Backend/` solo tenía `package.json` con `express`; `Frontend/` era el scaffold default de Vite+React. Nada de DB, router ni estilos existía.
- [x] Leído el mockup completo de "Crear / Editar Usuario" vía Claude Design MCP (paleta, tipografía, roles, permisos y sedes exactos).
- [x] Plan de implementación aprobado por el usuario (Prisma, solo CRUD sin auth, CSS plano, Docker).
- [x] Creado `docker-compose.yml` en la raíz (Postgres 16, DB `healthcore_dev`).
- [x] Creado `Backend/.env`, `.env.example`, `.gitignore`, `Backend/uploads/fotos/.gitkeep`.
- [x] Instaladas dependencias backend: `@prisma/client`, `bcryptjs`, `cors`, `multer`, `zod`, `dotenv` (+ dev: `prisma`).
- [x] Docker Desktop iniciado y `docker compose up -d` corriendo Postgres 16.
- [x] **Incidente:** `npx prisma init` instaló por defecto **Prisma 7.8.0**, que genera el cliente en **TypeScript puro** (`generated/prisma/*.ts`) vía el nuevo generador `prisma-client` + `prisma.config.ts`. El backend es JS plano (sin `tsx`/`ts-node`), así que el cliente generado no se podía `require()`. Se desinstaló (`npm uninstall prisma @prisma/client`), se borraron `generated/` y `prisma.config.ts`, y se fijó la versión estable **`prisma@6.19.3` / `@prisma/client@6.19.3`** (generador clásico `prisma-client-js`, cliente CommonJS en `node_modules/@prisma/client`). El schema y las migraciones ya escritas eran SQL estándar y no hubo que rehacerlas.
- [x] **Incidente:** al correr la primera migración, `docker compose` publicaba Postgres en el puerto **5432**, pero ese puerto ya estaba ocupado por una instancia **nativa de PostgreSQL** corriendo como servicio de Windows en esta máquina (proceso `postgres`, distinto del contenedor). Las conexiones desde Prisma caían en esa instancia nativa (con otras credenciales) y fallaban con `P1000 Authentication failed`. Se remapeó el contenedor al puerto **5433** (`docker-compose.yml` y `.env` actualizados) sin tocar la instancia nativa existente.
- [x] Prisma schema completo escrito (`Role`, `Sede`, `User`, `UserSede`, `UserPermission`, `AuditLog`) y migración inicial aplicada (`prisma/migrations/20260715032241_init`).
- [x] `prisma/seed.js`: 12 roles, 5 sedes (Cartagena/Norte/Bocagrande/Turbaco/Manga), 1 usuario admin semilla (`admin@healthhouse.co` / `Admin12345`, con permisos completos en los 6 módulos).
- [x] Backend completo: `src/config` (env con zod, prisma singleton, multer para fotos), `src/middleware` (validate, notFound, errorHandler con mapeo de errores Prisma P2002/P2025/P2003), `src/utils/audit.js`, `src/validators/usuarios.schema.js` (zod), `src/services/usuarios.service.js` (transacciones para reemplazar permisos/sedes en cada update), `src/controllers` y `src/routes` de roles/sedes/usuarios, `src/app.js`/`src/index.js`.
- [x] Verificado end-to-end por `curl`: `GET /health`, `GET /api/roles` (12), `GET /api/sedes` (5), `POST /api/usuarios` (multipart, crea usuario con permisos y sede), `GET /api/usuarios/:id` (confirma que `passwordHash` no se expone), `PATCH /api/usuarios/:id/estado` (desactivar), y 2 filas nuevas en `audit_logs` (`CREAR_USUARIO`, `DESACTIVAR_USUARIO`).
- [x] Frontend shell: `react-router-dom` instalado, proxy de Vite (`/api`, `/uploads` → `:4000`), Google Fonts en `index.html`, `styles/tokens.css` (paleta completa) + `styles/global.css` (primitivas compartidas: `.card`, `.input`, `.select`, `.btn*`, `.sectionHeader`, etc.), `Sidebar`/`Topbar`/`AppLayout` (12 ítems de nav fieles al mockup, submenú "Administración" con 5 hijos), `ComingSoonPage` para los módulos aún no construidos. Se limpió el demo default de Vite (`App.css`, `index.css`, contenido de `App.jsx`).
- [x] Formulario "Crear / Editar Usuario" completo: capa `api/` (`client.js` + `roles.api.js`/`sedes.api.js`/`usuarios.api.js`), constantes (`tiposDocumento`, `cargos`, `permisosModulos`), componente común `Toggle`, y las 5 secciones del mockup (`InfoUsuarioSection` con foto/contraseña/estado, `PermisosRapidosSection`, `AsignacionRolSection` con las 12 tarjetas de rol desde la API, `SedesPermitidasSection`, `InfoAdicionalSection` solo en modo edición) + `UserFormTabs` + `UserFormFooter`, orquestados por `UserForm.jsx` y cargados/hidratados por `UserFormPage.jsx`.
- [x] `npm run lint` (oxlint) del frontend: sin errores.
- [x] **Verificación end-to-end en navegador real** (Playwright headless, ya que no había `chromium-cli` disponible — se instaló `playwright` en el scratchpad de la sesión, reutilizando el Chromium ya cacheado en la máquina): se abrió `/usuarios/nuevo`, se confirmó que roles y sedes se cargan desde la API (no hardcodeados), se llenó el formulario completo (datos, rol, sede, contraseña) y se guardó → navegó a `/usuarios/:id/editar` con "Fecha de Creación" real e "Información Adicional" mostrando los datos correctos. Se confirmó en la base de datos que la contraseña quedó hasheada con bcrypt (60 caracteres) y que se crearon las 6 filas de permisos y la fila de sede. Se probó también "Desactivar Usuario" (cambia a "Inactivo", badge "Inactiva", botón pasa a "Activar Usuario") y quedó registrado en `audit_logs`. Se probó la navegación a un módulo no construido (Pacientes) confirmando que muestra `ComingSoonPage` sin romper la app. **Cero errores de consola** en todo el recorrido.

### 2026-07-15 / 2026-07-16 — Fase 0 (CSS) y Fase 1 (Pacientes)

Plan aprobado para implementar los 16 mockups restantes del proyecto Claude Design en 10 fases (flujo asistencial primero). Plan completo en `C:\Users\innovacion\.claude\plans\listo-el-m-dulo-humble-clarke.md`.

- [x] **Arranque en máquina nueva:** faltaba `Backend/.env` (gitignoreado; se creó desde `.env.example`) y el volumen Docker era nuevo (migración + seed desde cero). **Incidente:** en esta máquina el Postgres nativo de Windows ocupa los puertos **5432 y 5433**, y las conexiones IPv4 al 5433 caían en la instancia nativa (`P1000`). Se remapeó el contenedor al **5434** (`docker-compose.yml`, `.env`, `.env.example`).
- [x] **Fase 0 — Reorganización CSS:** los 13 `.module.css` co-ubicados se movieron a `Frontend/src/styles/{layout,common,users}/` (decisión del usuario: CSS separado del código por área; los módulos nuevos usan `styles/<modulo>/`). `tokens.css` y `global.css` quedan en la raíz de `styles/`. Verificado con lint + build + navegador. Commit `bace4aa`.
- [x] **Fase 1 — Migración `pacientes`:** modelos `Eps`, `Paciente` (demografía completa RF-PAC-01, aseguramiento RF-PAC-03, `sedeRegistroId`, foto RF-PAC-05), `ContactoEmergencia` (RF-PAC-04, cascade); enums `Sexo`, `EstadoCivil`, `GrupoSanguineo`, `Rh`, `Regimen`, `Zona`; `TipoDocumento` extendido con `RC/PE/PPT` (los exige RIPS). Seed idempotente: 15 EPS reales (`prisma/seedData/eps.js`) + 8 pacientes demo del mockup (`seedData/pacientes.js`).
- [x] **Fase 1 — Backend:** módulo pacientes a 4 capas (validator zod / service / controller con auditoría / routes) + `eps` thin. Endpoints: list paginado (`{data,total,page,pageSize}`) con filtros `documento`, `search`, `epsId`; `GET /stats`; `GET /:id`; `GET /:id/historial` (vacío hasta Fases 2-6); POST/PUT multipart con foto; PATCH estado. **Incidente/mejora:** la búsqueda por `contains` de Prisma no ignora acentos ("maria" no encontraba "María"); se agregó la extensión Postgres **`unaccent`** (migración `pacientes_unaccent`) y el filtro de nombre usa `unaccent(...) ILIKE unaccent(...)` vía `$queryRaw` (RF-PAC-02 búsqueda en tiempo real). `errorHandler` generalizado ("Registro no encontrado" en vez de "Usuario no encontrado").
- [x] **Fase 1 — Primitivas UI compartidas** (`components/common/` + `styles/common/`): `DataTable` (grid por config, paginación servidor, estados vacío/cargando), `Modal` (portal + Escape + click fuera), `ConfirmDialog`, `EstadoBadge` (variantes por token), `Tabs`. Tokens nuevos en `tokens.css` (warning-bg/text, neutral, amber, pink del mockup). `SearchSelect` se pospuso a Fase 2 (primer uso real: buscar paciente al crear cita).
- [x] **Fase 1 — Frontend Pacientes** (fiel a `Pacientes.dc.html` y `Pacientes - Perfil.dc.html`): `/pacientes` (4 tarjetas KPI desde `/stats`, filtros documento/nombre/EPS con debounce 300 ms, tabla de 9 columnas con badge de estado y acciones ver/editar, paginación), `/pacientes/nuevo` y `/pacientes/:id/editar` (formulario en 4 tarjetas: Información Personal con foto, Contacto y Residencia, Aseguramiento, Contactos de Emergencia repetibles — no hay mockup del formulario, se siguió el patrón del formulario de usuarios), `/pacientes/:id` (perfil con banda de encabezado, badge, meta y tabs Datos Generales / Aseguramiento / Contactos / Historial). `App.jsx` refactorizado con `IMPLEMENTED_PATHS`; breadcrumbs de pacientes en `pageHeader.js`; `/` ahora redirige a `/pacientes`. Nota: los KPI "Citas Hoy" y "Atenciones esta Semana" devuelven 0 hasta que existan Agenda (Fase 2) y HCE (Fase 4); "Última Atención" muestra "—".
- [x] **Fase 1 — Verificación:** smoke API de 12 checks (script Node en scratchpad): búsqueda sin acentos, filtros, paginación, crear con contactos, duplicado→409, fecha futura→400, PUT reemplaza contactos en transacción, estado, historial 404; 3 filas de auditoría (`CREAR/EDITAR/DESACTIVAR_PACIENTE`). E2E Playwright de 21 checks: recorrido completo lista→buscar→crear→perfil→tabs→editar (hidratación verificada)→desactivar con ConfirmDialog→buscar por documento. **Cero errores de consola.** Lint + build limpios. Pacientes de prueba eliminados; los 8 demo quedan.

### 2026-07-16 — Fase 2 (Agenda)

- [x] **Migración `agenda`:** modelos `Especialidad`, `Consultorio` (sede + especialidad + médico asignado, RF-ADM-04), `Cita` (paciente/médico/consultorio/sede, inicio/fin, estado, motivos de cancelación/reprogramación, self-relation `reprogramadaDeId`, índices por médico/consultorio/sede+inicio) y `Recordatorio` (stub RF-AGN-03); enums `EstadoCita` (6 estados RF-AGN-06), `CanalRecordatorio`, `EstadoRecordatorio`. **Anti-solape en dos capas (RF-AGN-04):** chequeo en servicio dentro de `$transaction` (mensaje amigable con el recurso en conflicto) + constraints de exclusión `EXCLUDE USING gist` sobre `(medicoId, tsrange(inicio,fin))` y `(consultorioId, tsrange(inicio,fin))` con `WHERE estado NOT IN (cancelada, no_asistio)` — **SQL agregado a mano** al `migration.sql` (Prisma no modela exclusiones; requiere extensión `btree_gist`). Violación 23P01 mapeada a 409 en `errorHandler` (detectada por nombre de constraint, Prisma no la tipifica).
- [x] **Seed:** 10 especialidades, 6 consultorios (3 sedes), 2 médicos demo (`torres@healthhouse.co` psicóloga / `vargas@healthhouse.co` médico general, `Demo12345`) y 9 citas demo relativas a hoy en todos los estados (idempotencia: solo si la tabla está vacía, las fechas son relativas).
- [x] **Backend:** `citas.schema.js` (zod con refine fin>inicio, cancelar exige motivo, reprogramar exige motivo), `agenda.service.js` (máquina de estados agendada→confirmada→en_atencion→atendida / cancelada / no_asistio con transiciones validadas → 409; reprogramación in-place con motivo y vuelta a `agendada`; recordatorio `pendiente` creado al agendar — envío real queda `// STUB SMS/email`), `agenda.controller.js` (auditoría `CREAR_CITA`, `REPROGRAMAR_CITA`, `CITA_<ESTADO>`), rutas `/api/citas` (list por rango/filtros, `/hoy`, POST, PUT `/:id/reprogramar`, PATCH `/:id/estado`) + catálogos thin `/api/especialidades`, `/api/consultorios?sedeId`, `/api/medicos` (users con rol clínico). `client.js` ganó `apiPostJson`/`apiPutJson`.
- [x] **Frontend** (fiel a `Agenda.dc.html`): `SearchSelect` común (autocomplete async con debounce, primer uso: buscar paciente por nombre o documento al agendar), tokens `--cita-*` (bg/border/text por estado, colores exactos del mockup), `CitaChip`, `CalendarGrid` (compartida por vistas Día y Semana: grilla 7:00–18:00, celdas de hora clicables que abren el formulario con fecha/hora prellenada, chips posicionados por hora/duración), `CalendarMonth` (6 semanas, máx 3 chips + "+n más", click en día → vista Día), `CitaFormModal` (paciente + sede→consultorio filtrado con médico autoseleccionado + fecha/hora/duración + motivo; el 409 de solape se muestra inline), `CitaDetalleModal` (detalle + acciones según máquina de estados + cancelar con motivo + reprogramar inline), `AgendaPage` (toolbar con chevrones/label de rango/toggle Día-Semana-Mes/filtro médico/Nueva Cita + panel lateral con leyenda de estados y "Citas de Hoy" clicables). Ruta `/agenda` registrada; breadcrumb "Agenda Médica".
- [x] **Verificación:** smoke API 18 checks (catálogos, rango+filtros, crear, solape médico→409, solape consultorio→409, misma franja con recursos distintos→201, fin≤inicio→400, transiciones válidas e inválida→409, cancelar sin motivo→400, reprogramar con/sin motivo); E2E Playwright 18 checks (vistas Día/Semana/Mes con chips reales, filtro por médico, crear cita desde el modal con SearchSelect, solape mostrando 409 inline, confirmar → badge, reprogramar con motivo → nota visible, cancelar con motivo). **Cero errores de consola** (excluyendo los 4xx intencionales de las pruebas de rechazo). Lint + build limpios. Citas demo restauradas al estado del seed.

### 2026-07-16 — Fase 3 (Admisiones)

- [x] **Migración `admisiones`:** modelo `Admision` (paciente, `citaId? @unique` — una cita solo se admite una vez, sede, médico, `tipoAtencion`, **snapshot** de `epsId`/`regimen` del paciente al momento de admitir, `numeroAutorizacion` RF-ADM2-02, `copago Decimal`, estado, `horaLlegada`, observaciones); enums `TipoAtencion {consulta, procedimiento, urgencia}` y `EstadoAdmision {en_espera, en_atencion, atendido, cancelado}`.
- [x] **Backend:** crear admisión desde cita o walk-in con validaciones a 409 (paciente inactivo, cita de otro paciente, cita ya admitida, cita cancelada/atendida); al admitir una cita `agendada` pasa a `confirmada` en la misma transacción; máquina de estados de la admisión que **sincroniza la cita ligada** (en_atencion→en_atencion, atendido→atendida, cancelado→cancelada con motivo "Admisión cancelada"). Rutas `/api/admisiones` (cola del día por sede/estado), `/espera` (RF-ADM2-03, orden por llegada), POST, PATCH `/:id/estado`. Auditoría `CREAR_ADMISION` / `ADMISION_<ESTADO>`. `GET /api/citas` ganó filtro `pacienteId` (el wizard busca las citas admisibles de hoy del paciente).
- [x] **Frontend** (fiel a `Admisiones.dc.html`): `Stepper` de 5 pasos con estados done/activo/pendiente y líneas de progreso; `AdmisionWizard` — Paso 1 Verificación (SearchSelect de paciente + datos + link "Corregir datos" + origen cita-de-hoy/walk-in con radio, cita única autoseleccionada), Paso 2 Tipo de Atención + sede + observaciones, Paso 3 Autorización EPS (nro + copago, textos del mockup), Paso 4 Médico (prellenado desde la cita), Paso 5 Confirmación con resumen; al confirmar → banner de éxito y reset. Panel lateral: "Paciente Verificado" (aparece al seleccionar) y "Lista de Espera" (badge amarillo con contador, posición numerada, minutos de espera). Debajo del wizard: tabla "Admisiones de Hoy" (DataTable con llegada/paciente/tipo/médico/origen/estado badge/acciones de la máquina de estados; cancelar pasa por ConfirmDialog). Ruta `/admisiones`.
- [x] **Verificación:** smoke API 15 checks (admisión desde cita, cita→confirmada, snapshot EPS, doble admisión→409, paciente inactivo→409, cita cruzada→409, walk-in, lista de espera ordenada, sincronización cita en cada transición, transición inválida→409, cancelación) + E2E Playwright 20 checks (wizard completo de 5 pasos con paciente real, autorización, resumen, admisión → lista de espera → Iniciar Atención → Atendido, badges actualizándose). **Cero errores de consola.** Lint + build limpios. Datos demo restaurados.

### 2026-07-16 — Fase 4 (Historia Clínica) — COMPLETA

- [x] Mockup `Historia Clinica.dc.html` leído vía Claude Design MCP (banda de paciente, cards Motivo/Anamnesis, Signos Vitales, Examen Físico, Diagnósticos CIE-10, Plan de Manejo, panel Trazabilidad + banner Res. 3100).
- [x] **Migración `historia_clinica`:** modelos `Cie10`, `Atencion` (desde admisión opcional `admisionId @unique`, motivo/anamnesis/antecedentes/examen/plan, estado `en_curso|cerrada|anulada`, sellos `cerradaAt`/`anuladaAt`), `SignosVitales` (1:1 cascade, TA/FC/FR/temp/peso/talla/IMC/SpO2), `AtencionDiagnostico` (cie10 + tipo principal/secundario/complicación + condición confirmado/impresión, `@@unique([atencionId, cie10Id])`).
- [x] Seed: **80 códigos CIE-10** representativos (`prisma/seedData/cie10.js`, upsert por código listo para catálogo completo).
- [x] **Backend:** `atenciones.schema.js` (zod; signos vitales con `.nullish()` para aceptar `null` explícito sin romper la coerción numérica; diagnósticos sin CIE-10 repetido vía `.refine`), `historiaClinica.service.js` (guard central `assertEditable` — solo `en_curso` es editable; crear desde admisión valida pertenencia/estado y pasa la admisión a `en_atencion` sincronizando la cita ligada; IMC calculado en servicio desde peso/talla; cerrar → `cerrada`+`cerradaAt` y admisión→`atendido`/cita→`atendida`; anular exige motivo y es posible desde `en_curso` o `cerrada`; **sin rutas DELETE**), `atenciones.controller.js` (auditoría `CREAR/EDITAR/CERRAR/ANULAR_ATENCION`), `atenciones.routes.js` (`GET /api/atenciones?pacienteId`, `GET /:id`, `GET /:id/trazabilidad` desde `audit_logs`, `POST /`, `PUT /:id`, `POST /:id/cerrar`, `PATCH /:id/anular`). Catálogo thin `GET /api/cie10?search=` (por código o descripción). `admisiones.service`/`.controller` ganaron filtro `pacienteId` y el include expone `atencion:{id}` (lo usa el modal de "Nueva Atención" para ofrecer solo admisiones sin atención registrada).
- [x] **Frontend** (fiel a `Historia Clinica.dc.html`): `constants/hce.js`, `api/historiaClinica.api.js` + `api/cie10.api.js`; `pages/HistoriaClinicaPage.jsx` (`/historia-clinica`: `SearchSelect` de paciente → timeline de atenciones + botón "Nueva Atención"), `components/historia-clinica/NuevaAtencionModal.jsx` (sede/médico/admisión pendiente opcional/motivo), `pages/AtencionPage.jsx` (`/historia-clinica/atencion/:id`: banda de paciente con badge de estado + "Guardar Atención", secciones Motivo/Anamnesis/Antecedentes, `SignosVitalesGrid` con IMC autocalculado en vivo, Examen Físico, `DiagnosticosSection` (tabla + agregar vía `SearchSelect` de CIE-10 + tipo/condición), Plan de Manejo, `TrazabilidadPanel` lateral (timeline desde `audit_logs` + banner Res. 3100), `AnularAtencionModal`; **solo lectura automática** cuando la atención no está `en_curso` — inputs deshabilitados, sin botones de guardar/cerrar/agregar). Rutas registradas en `App.jsx` (`IMPLEMENTED_PATHS` += `/historia-clinica`) y breadcrumb en `pageHeader.js`.
- [x] **Conectado lo que quedaba esperando esta fase:** `pacientes.service.getHistorial()` ahora devuelve atenciones reales (fecha, médico, tipo desde la admisión, diagnóstico principal, factura `null` hasta Fase 6); KPI `atencionesSemana` en `getStats()` (cuenta atenciones no anuladas desde el lunes); columna "Última Atención" de la lista de pacientes (`groupBy` de `Atencion` por `pacienteId`, excluye anuladas).
- [x] **Verificación:** smoke API de 24 checks (script Node en scratchpad: crear standalone, IMC calculado, editar, CIE-10 repetido→400, cerrar, editar/cerrar cerrada→409, anular sin motivo→400, anular→200, anular ya anulada→409, trazabilidad con 4 eventos, listado por paciente, crear desde admisión→admisión pasa a `en_atencion`, segunda atención misma admisión→409, cerrar→admisión pasa a `atendido`) + `DELETE` confirmado inexistente (404) + E2E Playwright headless (12/12 checks funcionales, cero errores de consola): buscar paciente → nueva atención → llenar signos vitales (IMC 22.4 en vivo) → agregar diagnóstico CIE-10 → guardar → cerrar (badge y solo-lectura persistente tras recargar) → anular (banner con motivo). Lint + build del frontend limpios. Datos de prueba (atenciones/admisiones creadas por los scripts) eliminados de la BD al terminar.

> **Nota de flujo:** en esta sesión el usuario pidió no hacer `git commit` (los commits y el push a `main` los hace él directamente, para no aparecer Claude como colaborador en GitHub). El código de la Fase 4 (y de las fases siguientes) queda en el working tree sin commitear.

### 2026-07-16 — Fase 5 (Fórmulas y Órdenes)

- [x] Mockup `Formulas y Ordenes.dc.html` leído: 5 pestañas (Fórmula Médica, Órdenes de Laboratorio, Remisiones, Incapacidades, Consentimientos) sobre un mismo layout de 2 columnas (documento a la izquierda + "Vista Previa Imprimible" a la derecha).
- [x] **Migración `formulas_ordenes`:** `Cups` (catálogo), `Formula`+`FormulaItem`, `Orden`+`OrdenItem` (cupsId + `PrioridadOrden{rutinaria,prioritaria}`), `Remision` (especialidad/IPS destino + justificación), `Incapacidad` (cie10Id + numeroDias + fechaInicio/Fin), `Consentimiento` (procedimiento + firmante + `EstadoConsentimiento{pendiente,firmado,anulado}` + `firmaUrl`); los 4 primeros comparten `EstadoDocumentoClinico{emitido,anulado}` + `anuladaMotivo`/`anuladaAt`; todos con `pacienteId`, `atencionId?` (valida pertenencia si se referencia), `medicoId`, `sedeId`. **Decisión de alcance:** sin estado "borrador" (el mockup lo insinúa con "Guardar Borrador", pero el plan solo pide "emitir/anular"); se crea directo en `emitido`. Seed: **85 códigos CUPS** representativos (`prisma/seedData/cups.js`, upsert por código).
- [x] **Backend:** `formulasOrdenes.schema.js` (zod para los 5 tipos, incapacidad con `.refine` fechaFin≥fechaInicio), `formulasOrdenes.service.js` (una función crear/listar/anular por tipo + `firmarConsentimiento`; `assertAtencionPertenece` valida que la atención referenciada sea del paciente), `formulasOrdenes.controller.js` (factory `crudDocumento()` para no repetir el boilerplate de list/get/create/anular en los 5 tipos, con auditoría `EMITIR_/ANULAR_<TIPO>`), rutas montadas en `/api/formulas`, `/api/ordenes`, `/api/remisiones`, `/api/incapacidades`, `/api/consentimientos` (+ `POST /:id/firmar` solo en consentimientos) y catálogo thin `GET /api/cups?search=`. `config/multer.js` ganó `uploadFirma` (PNG, `uploads/firmas/`, 512 KB máx) para la firma del consentimiento (canvas → Blob PNG, sin librería de firma). Sin rutas DELETE en ninguno.
- [x] **Frontend:** componente genérico `components/formulas-ordenes/DocumentoTab.jsx` (parametrizado con `listFn/createFn/anularFn/renderCampos/payloadDeCampos/renderPreview`) reutilizado por Fórmula/Orden/Remisión/Incapacidad (evita repetir 4 veces el layout de formulario+historial+vista previa); `ConsentimientoTab.jsx` aparte por el flujo de firma (`SignaturePad.jsx`, canvas HTML5 con captura mouse+touch → `canvas.toBlob('image/png')`); `MedicamentosEditor`/`OrdenItemsEditor` (líneas repetibles); `DocumentoHistorialList` + `AnularDocumentoModal` compartidos; `PrintLayout.jsx` + `styles/common/print.css` (`@media print` oculta todo excepto `.printArea`, `window.print()`, sin librería PDF — encabezado de IPS **hardcodeado** hasta que exista `Configuracion` en la Fase 6). `pages/FormulasOrdenesPage.jsx` (`/formulas-ordenes`: buscador de paciente + `Tabs` de 5 + la pestaña activa). Ruta registrada en `App.jsx` y breadcrumb en `pageHeader.js`.
- [x] **Verificación:** smoke API de 17 checks (emitir cada tipo, ítems vacíos→400, fechaFin<fechaInicio→400, anular→200, anular ya anulado→409, firmar→200 estado firmado, firmar ya firmado→409, anular consentimiento firmado→200, listado por paciente, DELETE inexistente→404) + E2E Playwright (9/9 checks: las 5 pestañas de punta a punta incluyendo dibujar y guardar la firma en el canvas y anular el consentimiento), cero errores de consola. Lint + build limpios. Datos y archivos de firma de prueba eliminados al terminar.

### 2026-07-16 — Fase 6 (Facturación + Caja)

- [x] Mockups `Facturacion.dc.html` y `Caja y Pagos.dc.html` leídos: lista de facturas con 4 KPI + filtros + botón "Generar Factura DIAN"; Caja con 3 tarjetas por método de pago, tabla de pagos de hoy + "Registrar Pago", panel "Cuadre de Caja" + botón "Cerrar Caja".
- [x] **Migración `facturacion_caja`:** `Convenio` (eps + `TipoContrato{evento,capitacion,paquete}`, `@@unique([epsId,tipoContrato])`), `Servicio` (catálogo facturable), `Tarifa` (`@@unique([convenioId,servicioId])`, valor+copago negociados), `NumeracionFactura` (consecutivo transaccional por prefijo), `Factura`+`FacturaItem` (numero null en borrador, `EstadoFactura{borrador,emitida,pagada,anulada,en_glosa}`, `estadoDian` **STUB** `'pendiente_emision'`), `NotaFactura` (crédito/débito), `Pago` (reciboNumero único, misma numeración transaccional con prefijo `RC`), `CierreCaja`, **`Configuracion`** (clave/valor Json). Seed: 15 servicios facturables, 7 convenios (EPS×tipoContrato) con tarifas derivadas para los 15 servicios (factor por tipo de contrato), numeraciones `FE`/`RC` en cero, y `Configuracion` clave `ips` con los datos de la IPS demo.
- [x] **Backend:** `facturacion.schema.js`/`.service.js`/`.controller.js` — crear factura en `borrador` calculando `valorUnitario` desde la `Tarifa` del convenio (409 si un servicio no tiene tarifa configurada); `POST /:id/emitir` asigna número vía `siguienteNumero()` transaccional (increment + validación de rango); `PATCH /:id/estado` solo permite `anulada`/`en_glosa` manualmente (transiciones validadas, `pagada` **no** es alcanzable por esta vía); `POST /:id/pagos` registra el pago con numeración `RC-######` y **transiciona automáticamente a `pagada`** cuando la suma de pagos cubre el total; `POST /:id/notas` (crédito/débito, informativa); `GET /stats` (facturado hoy, pendiente por cobrar, en glosa, emitidas del mes). Catálogos thin `/api/convenios` (+ `/:id/tarifas`) y `/api/servicios`. Caja: `GET /api/caja/resumen?sedeId=` (totales por método + pagos de hoy) y `POST /api/caja/cierres` (cierra los pagos del día no asignados a otro cierre, calcula `totalCaja = base + recaudado - egresos`, 409 si la sede ya cerró hoy). `Configuracion` thin CRUD (`GET /api/configuracion`, `GET/PUT /:clave`). Sin rutas DELETE.
- [x] **Frontend:** `constants/facturacion.js`, `api/facturacion.api.js` + `catalogosFacturacion.api.js` + `caja.api.js` + `configuracion.api.js`; `pages/FacturacionPage.jsx` (`/facturacion`: 4 KPI + filtros estado/búsqueda + `DataTable` + `FacturaFormModal` con tarifa en vivo por convenio), `pages/FacturaDetallePage.jsx` (`/facturacion/:id`: info general, ítems, notas, pagos, acciones contextuales al estado — Emitir/Anular/Marcar en Glosa/Registrar Pago/Nota — y `PrintLayout`), `pages/CajaPagosPage.jsx` (`/caja-pagos`: 3 tarjetas por método, tabla de pagos de hoy, `RegistrarPagoModal` compartido con búsqueda de factura, "Cuadre de Caja" + `CierreCajaModal`). **`PrintLayout.jsx` ahora lee el encabezado desde `GET /api/configuracion/ips`** en vez de estar hardcodeado (cierra el pendiente anotado en la Fase 5). Rutas registradas en `App.jsx` y breadcrumbs en `pageHeader.js`.
- [x] **Verificación:** smoke API de 27 checks (tarifa faltante→409, factura sin ítems→400, pago sobre borrador→409, emitir→número `FE-######`, emitir ya emitida→409, pago parcial no cierra, pago que completa el total→`pagada`, pago sobre pagada→409, anular pagada→409 transición inválida, marcar en_glosa→200, anular sin motivo→400, anular desde en_glosa→200, nota crédito→201, stats, resumen de caja agrupa por método, cerrar caja calcula el total, cerrar caja ya cerrada→409, `GET configuracion/ips`, DELETE inexistente→404) + E2E Playwright (9/11 checks automáticos con cero errores de consola; 2 aserciones de timing del propio script fallaron pero se confirmó por API directa que el estado `pagada` y el listado eran correctos). Lint + build limpios. Datos de prueba (facturas/pagos/cierres) eliminados y numeraciones reiniciadas a 0 al terminar.

### 2026-07-16 — Fase 7 (RIPS)

- [x] Mockup `RIPS.dc.html` leído: filtro período/sede + botones "Validar RIPS"/"Generar RIPS", 4 KPI (registros del período, validados sin errores, errores, advertencias), tabla "Resultado de Validación" (severidad/registro/descripción/estado). **Decisión de alcance:** se colapsan los dos botones del mockup en una sola acción "Generar y Validar RIPS" (cada generación ya valida y persiste el resultado), ya que el backend no modela un estado "solo vista previa" separado.
- [x] **Migración `rips`:** modelo `RipsExport` (`sedeId?`, `desde`/`hasta` date, `contenido` Json, `totalRegistros`, `erroresValidacion` Json, `EstadoRips{generado,validado_ok,validado_con_errores}`). Sin seed (se genera bajo demanda desde facturas reales).
- [x] **Backend:** `rips.schema.js` (refine hasta≥desde), `rips.service.js` — `generarRips({desde,hasta,sedeId})` recorre las `Factura` emitidas/pagadas del período (con sus `FacturaItem`+`Servicio`, `Paciente`, y `Admision→Atencion→AtencionDiagnostico` si existen) y arma un JSON **fiel a la estructura de la Res. 2275/2023 sobre un subconjunto representativo de campos** (no el Anexo Técnico completo): `usuarios` (US, uno por paciente único), `consultas` (AC, ítems cuyo código de servicio empieza por `CONS`/`URG`, con diagnóstico principal si la factura viene de una atención), `procedimientos` (AP, el resto de ítems); `numDocumentoIdObligado` se lee de `Configuracion` clave `ips`. Validaciones estructuradas generadas en el mismo paso: error si una consulta no tiene diagnóstico principal, advertencia si la factura no está ligada a una admisión (sin número de autorización), error si un paciente menor de edad tiene `tipoDocumento=CC`, correcto en el resto; `estado` de la exportación = `validado_con_errores` si hay algún error. Rutas `GET /api/rips` (historial), `GET /api/rips/:id`, `POST /api/rips/generar`. Sin rutas DELETE.
- [x] **Frontend:** `constants/rips.js`, `api/rips.api.js`; `pages/RipsPage.jsx` (`/rips`: filtro desde/hasta + sede, botón único "Generar y Validar RIPS", 4 tarjetas KPI, tabla de validación con ícono/color por severidad, "Descargar JSON" vía `Blob`+`URL.createObjectURL` — sin librería, y lista de "Generaciones Anteriores" clicable para recargar un resultado previo). Ruta registrada en `App.jsx` y breadcrumb en `pageHeader.js`.
- [x] **Verificación:** smoke API de 15 checks (estructura US/AC/AP, conteos exactos por período, error por consulta sin diagnóstico, advertencia por factura sin admisión, estado `validado_con_errores`, rango vacío→`validado_ok` con 0 registros, `hasta<desde`→400, listado e historial) + E2E Playwright (6/6 checks: generar desde el navegador, ver las 4 tarjetas y la tabla, descargar el `.json` real y verificar su estructura), cero errores de consola. Lint + build limpios. Datos de prueba (facturas, RIPS generados) eliminados al terminar.

### 2026-07-16/17 — Fase 8 (Parametrización + Sedes/Consultorios)

- [x] Mockups `Parametrizacion.dc.html` (6 pestañas EPS/Especialidades/Servicios/Convenios/Tarifas/CUPS, tabla genérica + botón "Agregar") y `Sedes y Consultorios.dc.html` (grid de tarjetas de sede + tabla de consultorios) leídos.
- [x] **Migración `parametrizacion_sedes`:** `Sede` += `direccion`/`telefono`/`codigoHabilitacion`/`horarios` (Json); `Especialidad` += `codigo` (único, nullable). **Incidente:** `prisma migrate dev` falla en este entorno con "environment is non-interactive" apenas hay cualquier warning (incluso un unique constraint nullable inofensivo); workaround usado (y que se repitió en Fase 8): generar el SQL con `prisma migrate diff --from-schema-datasource ... --to-schema-datamodel ... --script`, crear a mano la carpeta en `prisma/migrations/<timestamp>_<nombre>/migration.sql`, y aplicar con `prisma migrate deploy` (no interactivo) + `prisma generate`.
- [x] **Backend:** se elevaron a CRUD completo los catálogos que hasta ahora eran thin/solo-lectura: `eps.controller.js` (create/update/estado, `?todas=true` para admin vs. solo-activas para el resto de la app), `catalogosAgenda.controller.js` (especialidades y consultorios ganan create/update/estado además del `medicos` thin que se mantiene), `catalogosFacturacion.controller.js` (servicios create/update/estado; convenios create/estado — sin editar eps/tipoContrato tras crearlo, `@@unique([epsId,tipoContrato])` → 409 si ya existe; tarifas vía `PUT /api/tarifas` upsert por `convenioId+servicioId`), `cups.controller.js` (create/update/estado). **`sedes` se elevó a 4 capas** (`sedes.service.js` nuevo): crear/editar con transacción que garantiza que solo una sede tenga `esPrincipal=true` a la vez (desmarca las demás). Todos los catálogos con estado usan el patrón `PATCH /:id/estado {activo}`; ninguno tiene ruta DELETE. **Incidente durante la verificación:** el checker de exclusividad de `esPrincipal` desmarcó sin querer la "Sede Principal" real de la semilla al probar una segunda sede principal de prueba — se restauró manualmente antes de continuar (buena captura de por qué esta exclusividad debe probarse con cuidado en datos demo).
- [x] **Frontend:** componente genérico `components/parametrizacion/CatalogoTab.jsx` + `CatalogoFormModal.jsx` (tabla + modal crear/editar + activar/desactivar, reutilizado por EPS/Especialidades/Servicios/CUPS vía props `listFn/createFn/updateFn/setEstadoFn/columns/renderCampos`), `ConveniosTab.jsx` (propio: crear con EPS+tipoContrato, solo activar/desactivar) y `TarifasTab.jsx` (propio: selector de convenio + tabla de todos los servicios con edición inline de valor/copago vía upsert). `pages/ParametrizacionPage.jsx` (`/administracion/parametrizacion`, 6 pestañas) y `pages/SedesConsultoriosPage.jsx` (`/administracion/sedes-consultorios`: grid de tarjetas de sede fiel al mockup + tabla de consultorios, ambos con su modal de creación/edición reutilizando `CatalogoFormModal`). Rutas registradas en `App.jsx` y breadcrumbs en `pageHeader.js` (el sidebar ya tenía ambos paths desde la Fase 0).
- [x] **Verificación:** smoke API de 23 checks (crear/editar/desactivar cada catálogo, filtros activo-only vs. `?todas=true`, código duplicado→409, convenio duplicado eps+tipoContrato→409, upsert de tarifa no duplica fila, exclusividad de sede principal) + E2E Playwright (11/11 checks: crear un registro en cada una de las 6 pestañas de Parametrización desde el navegador + crear sede y consultorio nuevos en Sedes y Consultorios), cero errores de consola. Lint + build limpios. Datos de prueba eliminados y "Sede Principal" restaurada como principal al terminar.

### 2026-07-17 — Fase 9 (Roles y Permisos + Auditoría + listado Usuarios)

- [x] Mockups `Roles y Permisos.dc.html` (tarjetas de rol + matriz de permisos por módulo) y `Auditoria.dc.html` (filtros + tabla de logs) leídos. **Decisión de alcance:** sin CRUD de roles en sí (crear/eliminar rol) — el catálogo de roles sigue siendo el sembrado por `seed.js`; esta fase solo agrega la **plantilla de permisos por rol** (`RolePermission`) y su aplicación a usuarios, tal como pide el plan.
- [x] **Migración `roles_auditoria`:** `RolePermission` (`@@unique([rolId,modulo])`, mismo shape que `UserPermission` pero a nivel de rol) y `AuditLog += ip`.
- [x] **Captura de IP sin tocar los ~14 controllers que ya llaman `writeAuditLog`:** middleware `withRequestContext` (nuevo `utils/requestContext.js`, usa `AsyncLocalStorage` de Node) montado temprano en `app.js`; `writeAuditLog` ahora lee el IP de ese contexto internamente — ningún call site existente cambió.
- [x] **Backend:** `roles.service.js` (`getPermisosDeRol` devuelve los 6 módulos siempre, con defaults en `false` para los que no tengan fila aún; `setPermisosDeRol` hace upsert transaccional de la matriz completa), `GET/PUT /api/roles/:id/permisos`; `GET /api/roles` ahora incluye `_count.users`. `usuariosService.aplicarPlantillaDeRol` (reemplaza `UserPermission` del usuario por la plantilla de su rol) → `POST /api/usuarios/:id/aplicar-plantilla`. `auditoria.service.js` (`listAuditLogs` paginado con filtros `entidad`/`accion`/rango de fechas — con fin de día exclusivo para que "hasta" incluya el día completo —, más `listEntidadesDistintas`/`listAccionesDistintas` para poblar los filtros dinámicamente sin hardcodear catálogos) → `GET /api/auditoria`, `/auditoria/entidades`, `/auditoria/acciones`. Sin rutas DELETE en ninguno.
- [x] **Frontend:** `pages/UsuariosListPage.jsx` (`/usuarios`: cierra la deuda pendiente — `DataTable` de usuarios con búsqueda, botón "Aplicar Plantilla" por fila y acceso a "Editar"; el sidebar y el breadcrumb de Usuarios apuntan aquí en vez de directo a "Nuevo Usuario"), `pages/RolesPermisosPage.jsx` (`/administracion/roles-permisos`: tarjetas de rol clicables con conteo de usuarios + matriz de checkboxes de 6 módulos × 6 permisos con "Guardar Cambios"), `pages/AuditoriaPage.jsx` (`/administracion/auditoria`: filtros módulo/acción/rango de fechas poblados desde los endpoints `distinct`, tabla con badge de color por tipo de acción vía heurística de prefijo — `constants/auditoria.js` —, Modal de detalle con el JSON completo, "Exportar Log" descarga `.json` vía Blob). Como no hay login, la columna "Usuario" siempre muestra "Sistema" (limitación conocida, documentada).
- [x] **Verificación:** smoke API de 16 checks (matriz de permisos completa/persistencia, aplicar plantilla transfiere los permisos correctos, filtros de auditoría por entidad/acción/rango de fechas, endpoints `distinct`) + E2E Playwright (9/9 funcionales confirmados — 8 en la primera pasada, el 9º vía verificación dirigida tras detectar que el fallo era el propio selector del test chocando con un `<option>` oculto del filtro, no un bug de la app), cero errores de consola. Lint + build limpios. **Incidente durante la verificación:** las pruebes de "Aplicar Plantilla" y de guardar la matriz de permisos tocaron datos demo reales (permisos de `torres@healthhouse.co` y la plantilla del rol Psicólogo); se restauraron manualmente al estado del seed al terminar.

### 2026-07-17 — Fase 10 (Dashboard + Reportes + Configuración) — ÚLTIMA FASE DEL PLAN

- [x] Mockups `Dashboard.dc.html` (saludo + 4 KPI + próximas citas + accesos rápidos + actividad reciente), `Reportes e Indicadores.dc.html` (4 KPI con tendencia + gráfico de barras + productividad por médico + reporte financiero por sede + export Excel/PDF) y `Configuracion.dc.html` (datos de la institución, seguridad, notificaciones, copias de seguridad) leídos.
- [x] **Sin migración nueva** (el modelo `Configuracion` ya existía desde la Fase 6). Solo se agregó seed para las claves `general`/`seguridad`/`notificaciones`/`backup` (`prisma/seedData/configuracion.js`) para que la página de Configuración no dispare 404 en una instalación nueva antes del primer guardado.
- [x] **Backend:** `dashboard.service.js` (`GET /api/dashboard/kpis`: citas hoy, pacientes activos, recaudo de caja hoy, lista de espera, próximas citas activas del día, actividad reciente desde `audit_logs` con ícono/color por entidad). `reportes.service.js` (`GET /api/reportes/indicadores?desde&hasta`: atenciones/ingresos/ocupación de agenda/productividad promedio del período **con tendencia vs. el período anterior de igual duración**; "ocupación de agenda" es una proxy — % de citas del período que no terminaron canceladas/no-asistió, ya que no se modelan cupos/horarios disponibles; atenciones por mes de los últimos 6 meses; productividad por médico; reporte financiero por sede (facturación/recaudo/cartera/ocupación) — todo agregado con Prisma `count`/`aggregate`, sin `groupBy` nativo por mes ya que Postgres necesitaría `date_trunc` crudo). `GET /api/reportes/exportar.xlsx` genera un libro real de 3 hojas (Resumen/Productividad/Reporte por Sede) con **`exceljs`** (única dependencia nueva, instalada solo en Backend). `Configuracion` ya tenía CRUD genérico thin desde la Fase 6, reutilizado tal cual.
- [x] **Frontend:** `pages/DashboardPage.jsx` (`/dashboard`), `pages/ReportesPage.jsx` (`/reportes`: gráfico de barras y barras de progreso en **CSS puro, sin librería de gráficos**; envuelve el contenido en `.printArea` para reutilizar el mecanismo de impresión de `print.css` de la Fase 5 — botón "PDF" = `window.print()`; botón "Excel" descarga el `.xlsx` real vía Blob), `pages/ConfiguracionPage.jsx` (`/configuracion`: switches propios de 46×26px fieles al mockup, un `PUT` por cada una de las 4 claves editables al guardar). **`/` ahora redirige a `/dashboard`** (antes redirigía a `/pacientes`); `sidebarConfig.js`/`App.jsx` quedan con **las 16 rutas del sidebar implementadas — no queda ningún `ComingSoonPage` activo** (el componente se deja en el código por si se necesita en el futuro, pero ninguna ruta actual lo renderiza).
- [x] **Verificación:** smoke API directo (KPIs del dashboard con datos reales, indicadores con tendencias correctas — ocupación 78% verificada a mano contra las citas demo reales —, archivo `.xlsx` válido reconocido por `file` como "Microsoft Excel 2007+") + E2E Playwright de 14 checks: `/`→`/dashboard`, las 3 páginas nuevas con sus datos reales, descarga real del Excel, guardar Configuración con confirmación, **y un recorrido por las 16 rutas del sidebar confirmando que ninguna cae en ComingSoonPage**. Cero errores de consola (tras sembrar las claves de Configuración faltantes — antes de eso salían 404 esperados en una instalación nueva sin ese seed). Lint + build limpios.

## 🎉 Plan de 10 fases completo

Con esta fase se cierran las 10 fases de `docs/PLAN-IMPLEMENTACION.md`: Pacientes, Agenda, Admisiones, Historia Clínica, Fórmulas y Órdenes, Facturación + Caja, RIPS, Parametrización + Sedes/Consultorios, Roles y Permisos + Auditoría + Usuarios, y Dashboard + Reportes + Configuración. Los 17 mockups del proyecto Claude Design (`5e1ee8b8-...`) están implementados. Quedan documentadas como decisiones de alcance conscientes (no como deuda): sin login/JWT (decisión previa del usuario), CIE-10/CUPS como subconjuntos representativos (~80-85 códigos, upsert listo para catálogo completo), stubs marcados para integraciones externas (Siigo/DIAN, envío SMS/email, backups), y algunas simplificaciones puntuales documentadas en cada fase (p. ej. sin "borrador" en Fórmulas y Órdenes, sin CRUD de roles en sí, "ocupación de agenda" como proxy).

> **Nota importante:** por pedido del usuario, ninguna de las Fases 4-10 tiene `git commit` — todo el código queda en el working tree para que él lo revise y commitee/pushee manualmente (para no aparecer Claude como colaborador en GitHub).

> **Para retomar en otra máquina:** ver `docs/HANDOFF.md` (setup completo, estado, pendientes) y `docs/PLAN-IMPLEMENTACION.md` (plan aprobado de las 10 fases).

## Cómo levantar el proyecto

```bash
docker compose up -d                  # Postgres 16 en localhost:5434
cd Backend && npm run dev             # API en http://localhost:4000
cd Frontend && npm run dev            # App en http://localhost:5173 (proxy a la API)
```

Usuario admin semilla: `admin@healthhouse.co` / `Admin12345` (no hay login todavía, es solo para referencia futura).

## Próximos pasos sugeridos

Las 10 fases del plan están completas (ver sección "🎉 Plan de 10 fases completo" arriba). Ideas para una próxima iteración, ninguna es deuda de lo planeado:

- **Login/JWT y protección de rutas** — decisión consciente dejada fuera de estas 10 fases; los permisos por módulo (`UserPermission`/`RolePermission`) ya están modelados y listos para aplicarse en cuanto exista sesión de usuario.
- **CIE-10 y CUPS completos** — hoy son subconjuntos representativos (~80-85 códigos) con upsert por código; cargar el catálogo oficial completo es solo ampliar `prisma/seedData/{cie10,cups}.js`.
- **Integraciones externas reales** — Siigo/DIAN (`Factura.estadoDian`), envío de SMS/email (`Recordatorio`), backups (`Configuracion` clave `backup`) están modeladas con su estado en BD; falta conectar el proveedor real cuando el negocio lo requiera.
- **CRUD de roles en sí** (crear/eliminar rol, no solo su plantilla de permisos) si el negocio necesita más roles que los sembrados.
